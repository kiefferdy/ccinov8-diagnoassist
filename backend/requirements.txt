# ==================================================
# DiagnoAssist Requirements
# ==================================================

# Core FastAPI (keep versions for stability)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart

# Database (keep core versions)
sqlalchemy[asyncio]==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
supabase==2.3.0

# FHIR and Validation (let pip resolve pydantic)
fhir.resources==7.0.2
pydantic  # Let pip choose compatible version

# Authentication and Security
python-jose[cryptography]
passlib[bcrypt]
cryptography

# Configuration
python-dotenv==1.0.0

# AI Integration (core functionality)
openai
anthropic

# HTTP Client
httpx
requests

# Essential utilities
python-dateutil

# Development Tools (no version conflicts)
pytest
pytest-asyncio
black
isort
flake8

# File handling
aiofiles

# Background tasks (optional - remove if conflicts)
# celery
# redis

# Removed packages that cause pydantic conflicts:
# - fastapi-mail (pydantic >=2.0 conflict with fhir.resources)
# - pydantic-settings (version conflicts)
# - many optional packages

# Data processing (let pip resolve)
pandas
numpy

# Optional packages (no version pins)
structlog
python-json-logger
jsonschema
pytz
openpyxl
cachetools

# Production essentials
gunicorn